# Chatterbox TTS Core Package with multilingual support
# chatterbox-tts==0.1.4
chatterbox-tts @ git+https://github.com/travisvn/chatterbox-multilingual.git@exp

# Required for Chatterbox TTS watermarker functionality
resemble-perth

# PyTorch with CPU support (ensure CPU compatibility)
torch>=2.0.0,<2.7.0
torchaudio>=2.0.0,<2.7.0

# FastAPI and ASGI server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Required for FastAPI file upload support
python-multipart>=0.0.6

# Server-Sent Events support for real-time progress updates
sse-starlette>=3.0.2

# Environment variables
python-dotenv>=1.0.0

# Memory monitoring
psutil>=5.9.0

# Audio processing for long text concatenation
pydub>=0.25.1

# Testing Dependencies
requests>=2.28.0 


⚙️ Step 1: Uninstall CPU-only PyTorch

In your virtual environment:

pip uninstall torch torchvision torchaudio -y

⚙️ Step 2: Install the correct GPU-enabled PyTorch

Since your driver supports CUDA 12.9, you should install PyTorch built for CUDA 12.1, which works perfectly fine (PyTorch doesn’t yet release CUDA 12.9 wheels).

Run:

pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121


That will take a few minutes — it’s around 3–4 GB of packages.

⚙️ Step 3: Verify the installation

After installation completes, run:

python -c "import torch; print(torch.__version__); print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'No GPU')"


✅ Expected output:

2.4.1
True
NVIDIA GeForce GTX 1650


If you see that, PyTorch is now properly connected to your GPU.